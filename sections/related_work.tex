\section{Related Work}
\label{sec:related}

\paragraph{Importance and masking in continual learning.}
EWC~\citep{kirkpatrick2017overcoming}, SI~\citep{zenke2017continual}, and MAS~\citep{aljundi2018memory} estimate parameter importance for regularization, while GPM~\citep{saha2021gpm}, OGD~\citep{farajtabar2020ogd}, and GORP~\citep{wang2025gorp} constrain updates to gradient subspaces. Task-specific masks and sparse LoRA subspaces likewise assume that coordinate identity matters~\citep{serra2018hat,yang2025nlotools,wang2025piece}. We test this assumption within a fixed LoRA pool and budget; the result complements evidence that parameter-space gradients can be noisy attribution coordinates~\citep{li2026rept}.

\paragraph{Selection rules and PEFT.}
Pruning methods score weights before or after training~\citep{lee2019snip,wang2020grasp,frantar2023sparsegpt,sun2024wanda}, whereas random masking can already recover strong PEFT subnetworks~\citep{su2024random}. LoRA-FA~\citep{zhang2023lorafa} freezes \texttt{lora\_A} and trains \texttt{lora\_B}; our setting instead compares random and gradient-selected subsets within standard LoRA. Non-zero initialization methods such as PiSSA~\citep{meng2024pissa} fall outside our $B=0$ mechanism, as do adaptive or orthogonal per-task subspaces~\citep{wang2023olora,liang2024inflora}.

\paragraph{Closest concurrent work.}
Attribution-Guided CL~\citep{liu2026attributionguided} is the closest use of gradient attribution as an LLM continual-learning mask, although its published description does not establish all details needed to classify the pool comparison. Related work studies low-rank gradient projection~\citep{zhao2024galore,choe2024logra}, adapter redundancy~\citep{dieudonne2026adapter}, and random sparse subnetworks~\citep{adewuyi2026multiple}. Our contribution is the controlled gradient-versus-random comparison within a shared zero-initialized LoRA pool.


% \section{Related Work}
% \label{sec:related}

% Gradient- or saliency-scored importance is the shared assumption behind two otherwise separate lines. In regularization-based continual learning, EWC~\citep{kirkpatrick2017overcoming}, SI~\citep{zenke2017continual} and MAS~\citep{aljundi2018memory} score parameters by Fisher information or output sensitivity and penalize movement in the coordinates that score highest, and a separate line constrains updates to a gradient subspace (GPM~\citep{saha2021gpm}, OGD~\citep{farajtabar2020ogd}, GORP~\citep{wang2025gorp}, Model Shapley~\citep{xu2025modelshapley}). All of them assume that \emph{which} parameters are protected matters, and so do we; the question we put to that assumption is whether the selection criterion within the trainable pool has any measurable consequence once the pool and the budget are fixed (\S\ref{sec:confounded}, \S\ref{sec:d1}). This complements RepT~\citep{li2026rept}, which argues that parameter-space gradients are a noisy coordinate system for attribution.

% Our setting fixes the chosen fraction and varies only the ranking rule over a shared pool, which isolates the criterion from the budget. Under one protocol we train and evaluate four rules, Grad-Full, Grad-B, Random-B and Random A+B, and compare the remaining rules by their published design only, with none of their reported numbers presented here as a measurement of ours; Table~\ref{tab:rule_positioning} in Appendix~\ref{app:positioning} lists them side by side. That separation also distinguishes rules deciding \emph{which} parameters to protect from rules deciding \emph{how many}: the pruning-at-initialisation scorers (SNIP~\citep{lee2019snip}, GraSP~\citep{wang2020grasp}), the one-shot pruning scorers (SparseGPT~\citep{frantar2023sparsegpt}, Wanda~\citep{sun2024wanda}, movement pruning~\citep{sanh2020movement}) and LoRA-FA~\citep{zhang2023lorafa} all fix their subset once and never revisit it, and pruning-at-initialisation scores are largely interchangeable with a per-layer choice~\citep{frankle2021pruning}. Mask-learning variants instead adapt a fixed backbone per task~\citep{mallya2018piggyback} and serve as CL task-specific subnetworks~\citep{kang2022winning}; Wanda importance is nearly orthogonal to gradient importance ($\rho = 0.086$).

% Several methods show that parameter selection can improve continual learning: HAT~\citep{serra2018hat} learns per-task masks, \citet{yang2025nlotools} reduce inter-task collision via sparse LoRA subspaces, \citet{wang2025piece} update only 0.1\% of parameters per task, \citet{lin2025sparsememory} keep only uniquely relevant memory slots, \citet{deng2024darex} revisit delta-parameter pruning, and \citet{su2024random} show that random masking already discovers winning tickets for PEFT. Our finding does not contradict these; under our conditions the identity of the selected 10\% matters less than the fact that 90\% are frozen. Within parameter-efficient tuning, LoRA~\citep{hu2021lora}, QLoRA~\citep{dettmers2023qlora} and PackNet~\citep{mallya2018packnet} constrain updates to parameter subsets, \citet{li2017learning} (LwF) uses distillation, and \citet{biderman2024lora} find that LoRA forgets less than full fine-tuning. LoRA-FA~\citep{zhang2023lorafa} freezes \texttt{lora\_A} and trains only \texttt{lora\_B}, whereas we freeze a random or gradient-selected subset of an otherwise standard LoRA; other variants refine the parameterization (rsLoRA~\citep{kalajdzievski2023rslora}, DoRA~\citep{liu2024dora}, AdaLoRA~\citep{zhang2023adalora}, LoRA+~\citep{hayou2024loraplus}) or enforce per-task subspace orthogonality (O-LoRA~\citep{wang2023olora}, InfLoRA~\citep{liang2024inflora}, Janus-LoRA~\citep{chen2026januslora}, MoRAM~\citep{lu2026moram}, ASO-LoRA~\citep{wang2026asolora}, SLoRA~\citep{yang2026slora}). PiSSA~\citep{meng2024pissa} sets $B \neq 0$ at initialization, so the $B=0$ mechanism our confound rests on is specific to standard zero-init LoRA, and most adjacent, Attribution-Guided CL~\citep{liu2026attributionguided} uses gradient attribution as a CL parameter-selection mask for LLMs, the class our within-\texttt{lora\_B} finding evaluates. GaLore~\citep{zhao2024galore} and LoGra~\citep{choe2024logra} reduce gradient memory and scale data valuation through gradient projection, and \citet{dieudonne2026adapter}, \citet{adewuyi2026multiple} and \citet{azghan2026latentlora} report low-rank adapter redundancy, random sparse subnetworks for RLVR, and gradient-free latent routing respectively.
